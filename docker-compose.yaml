version: '3.8'
services:
  db:
    image: mariadb:11
    container_name: ecommerce_db
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_DATABASE: ecommerce
    ports:
      - "3306:3306"
    volumes:
      - ./online_clothes_shop_db.sql:/docker-entrypoint-initdb.d/init.sql